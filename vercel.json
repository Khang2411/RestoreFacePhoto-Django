{
    "builds": [{
        "src": "gfpgan/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "3555mb", "runtime": "python3.9.6" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "gfpgan/wsgi.py"
        }
    ]
}